<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="ParentTheme" parent="android:Theme.DeviceDefault.NoActionBar" />

    <style name="Patch" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="ParentTheme">
        <item name="android:colorPrimary">@color/pinkPrimary</item>

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/avd_splash_icon</item>
        <item name="android:windowSplashScreenAnimationDuration">@integer/splash_screen_duration
        </item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!--        <item name="android:enforceStatusBarContrast">true</item>-->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!--   For android tv     -->
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <!--        <item name="android:windowTranslucentStatus">true</item>-->
        <!-- bug https://stackoverflow.com/a/48328471/1067596-->
    </style>

    <style name="AppTheme.Wallpaper">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <style name="Patch.Wallpaper">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <style name="AppThemeDialog" parent="android:Theme.DeviceDefault.Dialog.Alert">
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowMinWidthMajor">60%</item>
        <item name="android:windowMinWidthMinor">95%</item>
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/avd_splash_icon</item>

        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>

        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!--  todo: remove when it arrives in cmp  -->

    <!-- Stub style to workaround Window.setClipToOutline not being available on API 21 -->
    <style name="DialogWindowTheme" tools:override="true">
        <item name="android:windowClipToOutline">false</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!-- Style for decorFitsSystemWindows = false -->
    <style name="FloatingDialogWindowTheme" tools:override="true">
        <item name="android:windowClipToOutline">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- Override material3's ModalBottomSheet theme to enable background blur -->
    <style name="EdgeToEdgeFloatingDialogTheme" parent="android:Theme.DeviceDefault.Dialog" tools:override="true">
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowClipToOutline">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowElevation">0dp</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
