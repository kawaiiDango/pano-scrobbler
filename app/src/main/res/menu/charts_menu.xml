<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_collage"
        android:icon="@drawable/vd_mosaic"
        android:title="@string/create_collage"
        app:iconTint="?android:colorForeground"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_legend"
        android:icon="@drawable/vd_legend"
        android:title="@string/legend"
        app:iconTint="?android:colorForeground"
        app:showAsAction="never" />

</menu>